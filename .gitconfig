[user]
    name = lanceberge
    email = bergeron.lance6@gmail.com
[core]
    editor = "emacsclient -t"
    ignorecase = false
[alias]
    b   = branch
    co  = checkout
    s   = status
    l   = log

    c   = commit
    cm  = commit -m
    cam = commit -am
    ca  = commit --amend

    d   = diff
    dc  = diff --cached
    r   = reset
    rr  = reset --soft
    rh  = reset --hard

    rc = rebase --continue

    rmc = rm --cached

    rs  = restore --staged
    R   = restore

    p   = push
    f   = pull

    a   = add
    aa  = add -A

    sp  = stash push
    sP  = stash pop
    spu = stash push --include-untracked

    lag = log --oneline --all --decorate=short --graph
[init]
    defaultBranch = master
[pull]
    ff = only
    rebase = true
[github]
    user = lanceberge
[push]
    autoSetupRemote = true
    rebaseAmend = noPrompt
[rebase]
    autoStash = true
[checkout]
    defaultRemote = origin
    autoStash = true
