#!/usr/bin/env bash

## Local Settings
if hostname | grep -q 'amazon'; then
    export PATH=$PATH:$HOME/.toolbox/bin

    alias ssh-cd="ssh dev-dsk-belancev-2b-946c0bd6.us-west-2.amazon.com"
    alias crna="cr -n --all"
    alias cra="cr --all"
    
    alias mwk="mwinit -o && kinit"
    alias mwo="mwinit -o"

    # COCO beta AWS account
    alias ada-beta="ada credentials update --account=coco-network-design-workflow-beta --provider=conduit --role=IibsAdminAccess-DO-NOT-DELETE --once"
    alias ada-prod="ada credentials update --account=coco-network-design-workflow-na-prod --provider=conduit --role=IibsAdminAccess-DO-NOT-DELETE --once"

    # Brazil
    alias bb="brazil-build"
    alias bba="brazil-build apollo-pkg"
    alias bre="brazil-runtime-exec"
    alias brc="brazil-recursive-cmd"
    alias bw="brazil ws"
    alias bwcs="brazil ws clean && brazil ws sync"
    alias bwsuse="bws use --gitMode -p"
    alias bwscreate="bws create -n"
    alias bbr="brc brazil-build"
    alias bball="brc --allPackages"
    alias bbb="brc --allPackages brazil-build"
    export DYLD_LIBRARY_PATH=/opt/xpressmp/lib


     # export XPRESS=/opt/xpressmp/bin
     # . /Applications/FICO\ Xpress/xpressmp/bin/xpvars.sh  

elif hostname | grep -q 'MacBook'; then
    export PATH=$PATH:/Users/lance/code/misc-scripts/food/:/Users/lance/code/Get-Stock-Data

fi

## Arch
# Start x at login
# if systemctl -q is-active graphical.target && [[ ! $DISPLAY && $XDG_VTNR -eq 1 ]]; then
#       exec startx ~/dotfiles/.xinitrc
# fi
